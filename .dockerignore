.git
.gitignore
.env
.env.*
!.env.example
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# `data/*` (not `data/`) so places.json can be re-included: the seed file must ship in the
# image, while the local SQLite db/wal files next to it must not.
data/*
!data/places.json
*.db
*.db-wal
*.db-shm
Dockerfile
docker-compose.yml
.dockerignore
README.md
LICENSE
tests/
.pytest_cache/
